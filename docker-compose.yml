version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "8030:80"
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
    networks:
      - internal
    depends_on:
      - api
      - web

  api:
    image: test-api-image
    deploy:
      replicas: 2
    env_file:
      - .env
    networks:
      - internal

  web:
    image: test-web-image
    deploy:
      replicas: 2
    env_file:
      - .env
    networks:
      - internal
    depends_on:
      - api

networks:
  internal:
    driver: bridge